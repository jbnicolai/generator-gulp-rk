language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulp-rk' ]; then cd .. && eval "mv $currentfolder generator-gulp-rk" && cd generator-gulp-rk; fi

