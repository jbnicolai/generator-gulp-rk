{
  "name": "<%= _.slugify(appName) %>",
  "version": "0.0.0",
  "dependencies": {<% if (includeBootstrap) { %>
    "bootstrap-sass-official": "~3.1.1",<% } %><% if (includeBourbon) { %>
    "bourbon": "~4.0.0-rc.1",<% } %>
    "jquery": "2.1.0",
    "modernizr": "~2.7.2"<% if (includeNormalize) { %>,
    "normalize.css": "~3.0.1"<% } %>
  }
}

